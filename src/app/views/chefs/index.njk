{% extends "Área-Adm/layout1.njk" %}

{% block content %}
    <div class="HeaderList">
        <div>
            <h1 class>Gerenciar chefs</h1>
        </div>
        <div>
            <button class="rec"><a href="/admin/chefs/create">Novo Chef</a></button>
        </div>
    </div>

    <section class="listChef">
        {% for chef in chefs %}
             <div class="rec2">
                <img src="{{chef.src}}" alt="{{chef.nome_chef}}">
                <div class="">
                    <h5>{{chef.nome_chef}}</h5>
                    <a href="/admin/chefs/{{chef.id}}">
                        Visualizar
                    </a>

                </div>
            </div>
        {% endfor %}
    </section>

{% endblock content %}