{% extends "Área-Adm/layout1.njk" %}

{% block content %}

<section class="profile">
    <form action="/admin/profile?_method=PUT" method="POST" >
    <div class="home">
        <div>Bem vindo <h2>{{user.name}}!</h2></div>
        <button type="submit">Salvar</button>   
    </div>

    <div class="form">
        <div class="item">
            <div>Nome</div>
            <div class="input">
                <input type="text" name="name" value="{{user.name}}">
            </div>
        </div>

        <div class="item">
            <div>Email</div>
            <div class="input">
                <input type="text" name="email" value="{{user.email}}">
            </div>
        </div>

        <div class="item">
        <div>Senha</div>
        <div class="inputPRO">
            <input type="password" 
            name="password" 
            placeholder="Digite sua senha para atualizar" >

        </div>
    </div>

</form>

</section>

{% endblock content %}